# Copy to .env for local development. SQLite needs no server; the file is created
# by `npx prisma migrate dev`. For production, switch the schema datasource
# provider to postgresql and point this at a hosted Postgres URL.
DATABASE_URL="file:./dev.db"

# Secret used to sign session cookies (HMAC). Set a long random value in production.
SESSION_SECRET="change-me"
